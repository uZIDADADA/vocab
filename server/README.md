# Optional server

Reserved for future Cloudflare infrastructure. The Android self-test phase does
not require a Vocab-owned server: WebDAV sync can connect to the user's existing
Cloudflare Worker/R2 setup directly.

No credentials, endpoints, or deployed resources belong in this directory.
